import React from 'react'

const NotFoundPage = () => (
  <div style={{
      textAlign: 'center',
      marginTop: '25%',
    }}>
    <h1 style={{
      fontSize: '100px',
      transform: 'rotate(5deg)',
    }}>
    404
  </h1>

  <h2 style={{
    }}>
    This page does not exist. :(
  </h2>

  </div>
)

export default NotFoundPage;
